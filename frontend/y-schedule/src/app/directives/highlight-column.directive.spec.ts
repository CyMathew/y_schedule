import { HighlightColumnDirective } from './highlight-column.directive';

describe('HighlightColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightColumnDirective();
    expect(directive).toBeTruthy();
  });
});
